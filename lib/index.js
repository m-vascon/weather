const { currentTemperature } = require('./currentTemperature');
const { findCoordinates } = require('./findCoordinates');
const { temperatureSelector } = require('./functionSelector');

module.exports = {
    currentTemperature,
    findCoordinates,
    temperatureSelector,
}